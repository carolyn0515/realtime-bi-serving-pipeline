name: baseline_medium

conversion_probability:
  view_to_cart_min: 0.18
  view_to_cart_max: 0.58
  cart_to_purchase_min: 0.22
  cart_to_purchase_max: 0.68

behavior_delay:
  view_to_cart_min_seconds: 30
  view_to_cart_max_seconds: 1200
  view_to_purchase_min_seconds: 60
  view_to_purchase_max_seconds: 1800
  cart_to_purchase_min_seconds: 30
  cart_to_purchase_max_seconds: 600

stream_rate:
  mean_events_per_second_min: 2.0
  mean_events_per_second_max: 12.0
  min_sleep_seconds: 0.02
  max_sleep_seconds: 2.0
